{
  "name": "@fitcoach/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "pnpm --filter @fitcoach/ai build && vitest run --passWithNoTests && pnpm test:runtime-package",
    "test:runtime-package": "pnpm build && node runtime-package-smoke.mjs",
    "build": "tsc"
  },
  "dependencies": {
    "@fitcoach/ai": "workspace:*"
  }
}
