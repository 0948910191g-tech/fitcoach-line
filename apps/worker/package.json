{
  "name": "@fitcoach/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests && pnpm test:runtime-package",
    "test:runtime-package": "pnpm --filter @fitcoach/ai build && pnpm build && node runtime-package-smoke.mjs",
    "build": "tsc"
  }
}
